# README v0.1.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent (recommended) or compose:execute to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the README an accurate, concise entry point for the signed v0.1.2 release and current WebUI behavior.

**Architecture:** Keep README focused on product capabilities, formal release installation, source startup, configuration, and links to detailed operational documents. Use v0.1.2 in every release archive example and avoid duplicating deployment transaction details already maintained in `docs/deployment.md` and `docs/operations.md`.

**Tech Stack:** Markdown, npm workspace release scripts, systemd deployment documentation.

## Global Constraints

- Use the formal `mimo-code-webui-v0.1.2.tar.gz` archive name in README examples.
- Describe only signed formal release packages in installation instructions.
- Preserve the independently distributed public-key fingerprint `4e49ece0fe7f28708821437dd1268e425bca81b2418ea8fd195cce74fe5b43f8`.
- Keep detailed deployment, backup, rollback, and release-authoring procedures in `docs/`.
- Do not modify generated `dist-release/` artifacts.

---

### Task 1: Rewrite README Quick-Start Documentation

**Files:**
- Modify: `README.md`
- Verify: `README.md`, `package.json`, `docs/deployment.md`, `docs/operations.md`

**Interfaces:**
- Consumes: formal release artifact naming from `package.json` and package workflow from `docs/deployment.md`.
- Produces: a current README that directs operators to the correct signed v0.1.2 package and detailed operational references.

- [ ] **Step 1: Identify stale release examples and missing current behavior**

Run:

```bash
grep -nE '0\.1\.1|unsigned|Esc|运行中|tar\.gz|\.sig|sha256' README.md
```

Expected: all install and verification examples use the stale `v0.1.1` archive name; the feature summary lacks active-session recovery, follow-up prompts, and cancellation controls.

- [ ] **Step 2: Update README content**

Replace the existing README structure with these sections in order:

```markdown
# MiMo Code WebUI

## Features
## Install A Signed Release
## Run From Source
## Configuration
## Development
## Operations
## Documentation
## License
```

Include exactly these formal package verification commands:

```bash
sha256sum -c mimo-code-webui-v0.1.2.tar.gz.sha256

openssl pkeyutl -verify \
  -pubin \
  -inkey mimo-code-webui-release.pub \
  -rawin \
  -in mimo-code-webui-v0.1.2.tar.gz \
  -sigfile mimo-code-webui-v0.1.2.tar.gz.sig
```

Mention that active native sessions restore their running state after refresh, accept follow-up prompts while running, and can be cancelled with the square control or `Esc`.

- [ ] **Step 3: Check Markdown and release references**

Run:

```bash
grep -n '0\.1\.1' README.md
grep -n 'mimo-code-webui-v0.1.2.tar.gz' README.md
```

Expected: the first command returns no matches; the second returns every formal release archive example.

- [ ] **Step 4: Run documentation-adjacent verification**

Run:

```bash
npm run typecheck
```

Expected: server and web TypeScript checks pass with exit code 0.

- [ ] **Step 5: Commit**

```bash
git add README.md docs/compose/plans/2026-07-13-readme-v0.1.2.md
git commit -m "docs: refresh v0.1.2 README"
```
